version: 2
updates:
  # GitHub Actions version pins.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: monthly
    groups:
      # Batch minor/patch action bumps into one PR; majors open individually.
      github-actions-minor-patch:
        patterns: ["*"]
        update-types: [minor, patch]
